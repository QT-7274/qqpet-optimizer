# ASAR 参考组件清单

该目录保留原应用的主进程、初始化模块、服务、窗口脚本和静态资源。组件价值主要是对照，不是直接复用。

| 类别 | 用途 |
|---|---|
| 主进程与初始化 | 对比启动顺序、全局对象和 Store 恢复 |
| 窗口与 renderer | 对比原始交互、IPC 和页面行为 |
| 宠物动作逻辑 | 对比 SWF 路由和状态机 |
| 服务模块 | 识别被移除、替换或隐私加固的能力 |
| 静态资源 | 检查移植版资源完整性 |

复用原则：先提取行为要求，再在当前代码中重新接入；不要让发布流程依赖此目录。
