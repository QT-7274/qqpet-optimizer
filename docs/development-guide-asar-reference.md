# ASAR 参考代码使用指南

## 用途

`qq_pet_asar/` 只用于定位原始实现和比较行为。不要直接在这里开发当前功能，也不要把它加入发布依赖。

## 推荐流程

1. 在参考目录定位原始窗口、状态或动作逻辑。
2. 记录可观察行为，而不是直接复制全部实现。
3. 在 `qq-pet-macos/` 或 `src/qq_pet/` 找到当前边界。
4. 以最小差异移植，并保留跨平台和隐私加固。
5. 在当前应用中完成回归验证。

## 验证重点

比较相同条件下的 Store 字段、SWF 路径、动作结束条件、窗口交互和错误处理。原版依赖版本不作为当前构建成功标准。
